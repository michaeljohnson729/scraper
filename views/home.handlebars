<div class="header">
    <h1>NY Times Scraper!</h1>
</div>
<div class="container">
    <button id="scrape-button">Scrape Articles!</button>
    <div class="articles">
        {{#each article}}
        <div class="article-data">
            <p>People: {{title}}</p>
            <p>Link: {{link}}</p>
            <button id="edit-button" data-id="{{_id}}">Add Note</button>
        </div>
        {{/each}}

    </div>
</div>